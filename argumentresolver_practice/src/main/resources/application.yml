spring:
  application:
    name: argumentresolver_practice

  datasource:
    url: jdbc:mysql://localhost:3306/movie
    username: root
    password: '1234'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      format_sql: true

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace
    root: info